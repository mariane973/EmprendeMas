import 'package:flutter/material.dart';

class HomeVendedor extends StatelessWidget {
  const HomeVendedor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Hola Vendedor")),
    );
  }
}
